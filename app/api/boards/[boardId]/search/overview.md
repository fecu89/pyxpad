# 보드 게시물 검색 API

`GET /api/boards/[boardId]/search?q=...`는 최초 SSR 페이지에 포함되지 않은 게시물까지 서버에서 제목·본문을 검색해 최대 50개를 반환한다.

보드 읽기 권한을 다시 검사하고, 승인 권한이 없는 사용자는 공개된 게시물과 본인이 작성한 대기·거절 게시물만 볼 수 있다. 첨부 메타데이터, 사용자 정의 필드, 댓글 수, 키별 반응 집계와 현재 사용자의 반응을 보드 DTO와 같은 형태로 반환한다.

