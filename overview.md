# PyxPad 프로젝트 개요

PyxPad는 학교 구성원이 패드에 글·첨부·댓글·반응을 모으고, 교사와 관리자가 공개 범위·멤버·승인·보관 정책을 운영하는 Next.js 기반 협업 서비스입니다.

## 공개 홈과 작업공간

- `/`는 로그인 없이 볼 수 있는 공개 랜딩입니다. 서비스 설명과 학생·교사 이용 흐름, 반응형 CSS 패드 예시를 제공하며 사용자·패드 DB는 조회하지 않습니다.
- 로그인 후 “내 패드”는 `/dashboard`입니다. 즐겨찾기·검색·보관·폴더·마이페이지와 같은 `app/(dashboard)/layout.tsx`를 공유해 이동 중에도 사이드바와 알림 연결이 유지됩니다.
- 보호 경로의 비로그인 사용자는 `/?login=1&callbackUrl=...`로 이동합니다. 공개 홈이 일반 로그인·회원가입·카카오 모달을 열고, 검증된 내부 경로로만 로그인 후 복귀시킵니다.

## 계정과 소속

- 카카오는 제공자가 검증한 이메일, 일반 계정은 영문자·숫자 `loginId`와 비밀번호로 로그인합니다. 두 로그인 식별자는 공통 `loginIdentifierLookup` HMAC과 `loginIdentifierEncrypted` AES-GCM 필드에 보관하되 API와 DTO는 `loginType`으로 둘을 구분합니다. 닉네임·프로필 URL도 암호화하고 일반 계정 비밀번호는 salt+scrypt 단방향 해시만 저장합니다.
- 일반 계정은 우회 세션 없이 NextAuth Credentials 인증을 사용합니다. 3~20자 아이디 중복 확인과 강한 비밀번호 검증 후 계정을 만들고, 최초 가입 화면에서 닉네임 중복 확인·프로필 사진·가입 유형·학교 소속을 설정합니다. `userId`는 내부 DB 기본키이므로 사람이 입력하는 API 필드는 `loginId`로 통일합니다.
- 로그인 시도는 PostgreSQL에서 IP·계정·IP+계정 버킷으로 각각 제한하고 계정 실패가 반복되면 점진적으로 대기 시간을 늘립니다. 성공 시 계정 실패 상태를 초기화하며 인증 이벤트에는 로그인 식별자·IP 원문 대신 HMAC만 남깁니다.
- 학생은 학교 → 학년 → 반을 고르고 1~99 사이의 출석번호를 입력해 즉시 가입합니다. 같은 반의 번호는 DB 고유 인덱스로 중복을 막으며, 학생도 패드를 직접 만들고 소유할 수 있습니다. 학생 소유 패드는 보관된 패드까지 포함해 계정당 최대 10개입니다.
- 관리자는 별도 `학생 계정 발급` 화면에서 학교·학년·반·번호·이름 XLSX 명단과 영문자·숫자 전용 아이디 접두어를 입력해 학교 → 학년 → 반 계층과 학생 Credentials 계정을 한 번에 만들 수 있습니다. 예를 들어 `ch`, 3학년 1반 6번은 `ch30106` 아이디가 됩니다. 일반 회원과 발급 학생 모두 같은 아이디 로그인 화면을 사용합니다.
- 일괄 생성 계정의 초기 비밀번호는 이름 첫 글자와 학생 코드로 만들되 평문을 저장하지 않고 관리자에게 한 번만 전달합니다. 첫 로그인은 `/change-password`로 제한되며 새 강한 비밀번호를 설정하면 모든 기존 세션이 해제됩니다.
- 교사 신청자는 학교의 `DEPARTMENT`를 선택하지만 실제 역할은 승인 전까지 `STUDENT`로 유지됩니다. `TeacherApprovalRequest`가 대기 중인 동안 `/approval-pending` 외 일반 화면과 API 접근을 차단합니다.
- 해당 학교 대표교사 또는 전체관리자가 승인하면 `TEACHER` 역할, 학교, 부서와 가입 완료 시각을 한 트랜잭션으로 저장합니다. 반려 시 사유를 확인하고 재신청할 수 있습니다.
- 전체관리자·사용자 조회 권한이 있는 보조관리자와 학교 교사는 관리자 화면에서 학생 번호를 지정·변경·해제할 수 있습니다. 교사는 대표 여부와 관계없이 자기 학교 학생만 다루며, 일반 교사에게는 역할·상태·소속 구조 변경 권한이 열리지 않습니다.
- 관리자 첫 화면은 `학교 대시보드`입니다. 학교별 학생·교사·학급 수와 번호·학급 미지정 학생만 요약하고 사용자 명부, 소속 관리, 학생 계정 발급으로 연결합니다. 전출·재적 상태·담임·정원·진급 기능과 관련 DB 컬럼은 사용 목적이 없어 제거했습니다.
- 전체관리자, 관련 권한이 있는 보조관리자, 해당 학교 대표교사는 선택 학생을 다른 반으로 일괄 이동할 수 있습니다. 서버가 1~99번 중 빈 번호를 트랜잭션에서 자동 배정하며 이동 전후와 사유를 감사 로그에 남깁니다.
- 소속 관리에서 학교를 펼치면 학생 학급과 교사 부서를 분리해 보고, 전체관리자는 같은 영역에서 활성 교사를 학교 대표교사로 지정·해제할 수 있습니다. 정지·역할 변경·타 학교 이동·삭제 시 대표 권한은 자동 해제됩니다.
- 승인 상태는 JWT에 권한 자체를 저장한 결과가 아니라 매 세션 갱신 시 DB에서 다시 계산합니다. 따라서 로그아웃·브라우저 종료 후에도 동일한 계정으로 로그인하면 승인된 역할과 소속이 유지됩니다.

## 주요 영역

- `app/page.tsx`, `components/landing`: 공개 랜딩과 로그인 진입점
- `app/(dashboard)`: 내 패드·즐겨찾기·검색·폴더·마이페이지가 공유하는 영속 사이드바 셸
- `app/onboarding`, `app/approval-pending`: 신규 학생 가입과 교사 승인 대기
- `app/b/[slug]`: 패드 본문·게시물 상세·발표·인쇄
- `app/admin`: 학교 대시보드·사용자·소속/대표교사·학생 계정 발급·교사 가입 요청·감사 로그 관리
- `app/api`: 인증된 변경과 공개 읽기 Route Handler
- `lib`: 인증·권한·보드·사용자·파일·알림 도메인 로직
- `prisma`: PostgreSQL 스키마와 순차 마이그레이션

화면·API·서버 모듈·데이터 모델의 전체 연결은 `structure.md`, 폴더별 세부 정책은 각 폴더의 `overview.md`를 참고합니다.
