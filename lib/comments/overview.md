# 댓글 멘션 검증

`mentions.ts`는 클라이언트가 보낸 구조화된 사용자 ID가 실제 보드 참여자인지 서버에서 검증합니다. 보드 소유자·멤버·게시물 작성자·해당 게시물의 기존 댓글 작성자만 허용하며, 중복 ID와 보드 밖 사용자를 거부합니다.

표시 이름은 암호화되어 있고 중복될 수 있으므로 권한 판단에 사용하지 않습니다. 본문의 `@표시명`은 사람에게 보이는 텍스트이고, 알림 대상은 별도 `CommentMention` 행의 내부 사용자 ID가 기준입니다.
