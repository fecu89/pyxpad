# 알림·초대 링크·삭제 사용자 상태

인앱 알림과 보드 초대 링크의 첫 데이터 구조를 추가하고, 탈퇴 처리용 `UserStatus.DELETED`를 도입한다.

- `Notification`: 수신자·행위자·보드·게시물과 읽은 시각을 저장한다.
- `BoardInviteLink`: 역할·만료·사용 횟수·폐기 시각을 저장한다.
- 이 단계의 초대 토큰 평문 컬럼은 바로 다음 마이그레이션에서 `tokenHash`로 교체된다. 최종 스키마는 평문 토큰을 보관하지 않는다.

